---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  labels:
{{ include "common.labels.meta" . | indent 4 }}
spec:
  replicas: {{ .Values.replicacount }}
  selector:
    matchLabels:
{{ include "common.labels.match" . | indent 6 }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
{{ include "common.labels.meta" . | indent 8 }}
    spec:
      containers:
        - name: {{ .Values.image.repository | trimPrefix "is/" }}
          image: {{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.mode }}
          command: ["/bin/sh"]
          args: ["-c", "while true; do date; sleep 666; done"]
          {{- end }}
          ports:
            - containerPort: {{ .Values.port }}
          env:
            - name: NGINX_ID
              value: "{{ .Release.Name }}"
            - name: NGINX_INIT
              value: "{{ .Values.init }}"
{{- with .Values.envs }}
{{ toYaml . | indent 12 }}
{{- end }}
          volumeMounts:
            - name: etc-pki
              mountPath: /usr/local/etc/pki
              readOnly: true
            - name: www-{{ .Release.Name }}
              mountPath: /var/opt/nginx/{{ .Release.Name }}
{{- with .Values.mnts }}
{{ toYaml . | indent 12 }}
{{- end }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.port }}
            initialDelaySeconds: 15
            periodSeconds: 60
      volumes:
        - name: etc-pki
          hostPath:
            path: {{ .Values.base.etc }}/pki
            type: Directory
        - name: www-{{ .Release.Name }}
          hostPath:
            path: {{ .Values.base.var }}/{{ .Release.Name }}
            type: Directory
{{- with .Values.vols }}
{{ toYaml . | indent 8 }}
{{- end }}
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      automountServiceAccountToken: false
      enableServiceLinks: false
